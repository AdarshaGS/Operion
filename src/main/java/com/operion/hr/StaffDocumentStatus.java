package com.operion.hr;

/** Record lifecycle - deliberately separate from DocumentVerificationStatus, same
 * precedent as StudentDocumentStatus. */
public enum StaffDocumentStatus {
	ACTIVE,
	SUPERSEDED
}
